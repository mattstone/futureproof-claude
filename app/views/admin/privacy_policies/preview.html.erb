<!DOCTYPE html>
<html>
<head>
  <title>Privacy Policy Preview</title>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <%= stylesheet_link_tag :app %>
  <style>
    body {
      margin: 0;
      padding: 20px;
      font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, "Helvetica Neue", Arial, sans-serif;
    }
    
    .preview-header {
      background: #1e293b;
      color: white;
      padding: 15px 20px;
      margin: -20px -20px 20px -20px;
      display: flex;
      justify-content: space-between;
      align-items: center;
    }
    
    .preview-title {
      margin: 0;
      font-size: 18px;
      font-weight: 600;
    }
    
    .preview-note {
      font-size: 14px;
      opacity: 0.8;
    }
    
    .preview-content {
      max-width: 800px;
      margin: 0 auto;
    }
  </style>
</head>
<body>
  <div class="preview-header">
    <h1 class="preview-title">Preview: <%= @privacy_policy.title %></h1>
    <span class="preview-note">This is how it will appear on the public website</span>
  </div>

  <div class="preview-content">
    <div class="legal-page">
      <div class="legal-container">
        <h1><%= @privacy_policy.title %></h1>
        <p class="last-updated">Last updated: <%= @privacy_policy.formatted_last_updated %></p>

        <div class="legal-content">
          <%= raw @privacy_policy.rendered_content %>
        </div>

        <div class="legal-footer">
          <a href="#" class="back-link">← Back to Home</a>
        </div>
      </div>
    </div>
  </div>
</body>
</html>